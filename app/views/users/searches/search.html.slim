h1 Todo 検索結果: #{ @todos[1].count}個

/ h2.hidden 検索条件 : #{@search}
/ - @todos.to_a.each do |todo|
/   - if todo.user === current_user
/     = link_to edit_users_todo_path(todo)
/       ul.todos-info-self
/         li.user タイトル：#{todo.title}
/         li.user テキスト：#{todo.text}
/   - else
/       ul.todos-info
/         li.user タイトル：#{todo.title}
/         li.user テキスト：#{todo.text}
